<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itlinxi.dao.TempDao">
    <!--新增-->
    <insert id="add" parameterType="com.itlinxi.pojo.Temp">
        insert into t_temp(time,temp_num)
                      values
        (#{time},#{tempNum})
    </insert>
</mapper>